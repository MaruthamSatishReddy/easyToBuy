# Setup Guide

## Prerequisites
- Docker & Docker Compose
- Java 21
- Node.js 18+

## Running with Docker Compose
1. Navigate to `ecommerce/`
2. Run `docker-compose up -d`

## Manual Run
1. Start specific services via Maven: `./mvnw spring-boot:run`
2. Start Frontend: `npm run dev`
